{
  "name": "@sns-growth-bridge/adapters-sns-ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only SNS-AI adapter. Phase 1 skeleton only — does not enable accounts, cron, or provider calls.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  }
}
