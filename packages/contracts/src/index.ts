/**
 * Canonical growth-contract package identity.
 *
 * Phase 1 ships no schemas, parsers, or source-repository types.
 * Identity fields such as `creatorId` are intentionally undecided.
 */
export const PACKAGE_NAME = '@sns-growth-bridge/contracts' as const;

export const PACKAGE_PHASE = 1 as const;
