# `@sns-growth-bridge/identity-links`

Explicit operator-confirmed Cross-Product Account Links.

v1 identity is `workspaceId + socialAccountId` ↔ `accountId` on an explicit `platform`. Handle, `externalAccountId`, and `credentialKey` are never mapping keys. `creatorId` is not invented.

See `docs/phase7a/IDENTITY_LINK_DESIGN.md`.
