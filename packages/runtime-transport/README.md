# `@sns-growth-bridge/runtime-transport`

Read-only local JSONL loader for SNS-AI `history` / `metrics` / `human-feedback`.

Caller supplies explicit file paths. No path search, no writes, no network, no My-SNS DB. Reuses Phase 6 adapters.

See `docs/phase7a/READ_ONLY_TRANSPORT.md`.
