{
  "name": "@sns-growth-bridge/scoring",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure metric normalization and performance scoring. Phase 1 skeleton only — SNS-AI scorer is not ported yet.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  }
}
