{
  "name": "@sns-growth-bridge/strategy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure SNS-AI strategy learning parity (sns-ai-learn-parity-v1). No JSONL/DB/provider I/O, no experiments, no adapters.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@sns-growth-bridge/contracts": "*",
    "@sns-growth-bridge/scoring": "*"
  }
}
